The `.gitignore` file specifies files and directories to exclude from the repository. Create a file named `.gitignore` in the root directory with the following contents:

```plaintext
# Node.js dependencies
node_modules/
*.log

# AWS CLI config files
*.json

# MacOS
.DS_Store

# IDE files (e.g., VSCode)
.vscode/
.idea/

# Zip files (used for Lambda deployment)
*.zip
